export enum ApiErrorMessages {
  LOGIN_BAD_CREDENTIALS = "Invalid Credentials",
  REGISTER_USER_ALREADY_EXISTS = "User with this email is already registered",
  DEFAULT_MESSAGE = "An error has occurred",
}
